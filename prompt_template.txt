
You are a Fact-Checking Assistant.
Verify if the "Text to check" is consistent with the "Ground truth relations".

Input:
1. Text to check: {text}
2. Relations from text: {ans_relations_text}
3. Ground truth relations: {relations_text}

Instructions:
- If there is a contradiction, output a JSON with "hard_labels" (indexes) and "explanation".
- If it is correct, output empty lists.

Response format (STRICT JSON):
{
    "hard_labels": [],
    "marked_text": "Text with <inconsistent>tags</inconsistent>",
    "explanation": "Reasoning..."
}
